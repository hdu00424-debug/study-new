# 运维工程师进阶学习路线规划（以 Kubernetes 为核心）

> **适用对象**：正在系统运维岗位，同时接触 Kubernetes、Jenkins、网络等多个方向，学习精力分散、路线混乱，希望系统性进阶并兼顾 CKA 认证的运维工程师。  
> **核心策略**：先筑基再纵深，先工程化再体系化，先能动手再求精通。学习路线以 Kubernetes 为主线，容器基础和网络基础为前置支撑，Jenkins 作为 DevOps 自动化工具体系，分阶段独立推进，互不阻塞。

---

## 一、学习优先级排序与排序依据

| 优先级 | 方向 | 排序理由 |
|:------:|:-----|:---------|
| **P0** | **Linux 基础 + 容器基础（Docker）** | 一切运维和云原生的根基。不懂容器机制，k8s 的学习是空中楼阁。 |
| **P1** | **网络基础** | k8s 的网络模型（Service、CNI、DNS、Ingress）完全依赖网络知识。网络不通，一切排查无从下手。 |
| **P2** | **Kubernetes 核心概念 + 基础操作** | 主线目标。在掌握容器和网络后，k8s 的学习效率会大幅提升，避免"背命令但不懂原理"的低效状态。 |
| **P3** | **Jenkins / CI-CD 流水线** | 运维向 DevOps 进阶的必备工具。但如果容器和 k8s 基础薄弱，流水线出问题根本无法排查。 |
| **P4** | **监控、日志、故障排查** | 运维的"日常战场"。需要在已有真实环境后才能深入，否则只是纸上谈兵。 |
| **P5** | **安全、成本治理、性能优化** | 高阶运维能力。属于"锦上添花"，前置知识储备不足时学习性价比低。 |

**核心逻辑**：`底层原理 → 平台技术 → 自动化工程 → 可观测性 → 高阶治理`  
> 不要试图同时推进所有方向。当前精力分散的根源，是没有把"先决条件"和"并行模块"分清楚。

---

## 二、分阶段路线图（总览）

```
阶段一：筑基期（第 1 - 4 周）
├── Linux 核心原理（进程、文件系统、网络、存储）
├── Docker 容器基础（镜像、容器、网络、存储、Dockerfile）
└── 网络基础（TCP/IP、DNS、路由、NAT、四层/七层）

阶段二：k8s 入门（第 5 - 8 周）
├── 集群搭建（kubeadm 手动搭建，理解每个组件）
├── 核心资源对象（Pod/Deployment/Service/Ingress/ConfigMap/Secret）
├── 基础运维操作（kubectl、日志、排障、资源管理）
└── 网络与存储（CNI、StorageClass、PV/PVC）

阶段三：k8s 进阶 + DevOps 工具链（第 9 - 14 周）
├── 调度与资源管理（调度器、污点/容忍、资源配额、HPA/VPA）
├── 安全与权限（RBAC、NetworkPolicy、PodSecurity、Secrets）
├── Jenkins + GitLab CI 流水线（容器镜像构建、Helm 部署、滚动更新）
└── 中间件部署（MySQL、Redis、Nginx Ingress Controller）

阶段四：可观测性与故障排查（第 15 - 18 周）
├── Prometheus + Grafana 监控体系搭建
├── ELK/Loki 日志采集与查询
├── 链路追踪（可选：Jaeger）
└── 常见故障排查方法论（网络不通、Pod 调度失败、资源不足、镜像拉取失败）

阶段五：CKA 认证冲刺（第 19 - 22 周）
├── 官方考试范围逐项过
├── 模拟环境限时刷题
├── 高频操作手写强化（JSONPath、资源管理、集群排障）
└── 实战演练：集群升级、备份恢复、网络策略、权限配置
```

---

## 三、每个阶段核心知识点

### 阶段一：筑基期（第 1-4 周）

**目标**：为 Kubernetes 学习扫清所有前置障碍，确保后续学习不再因为"底层不懂"而卡住。

| 模块 | 核心内容 | 掌握标准 |
|:-----|:---------|:---------|
| Linux 核心 | 进程管理（fork/进程间通信）、文件系统（inode/VFS）、cgroups/namespace 基础 | 能用 cgroups 限制进程资源，理解 chroot/namespace 的隔离作用 |
| Docker 容器 | 镜像分层、容器生命周期、Dockerfile 编写、镜像构建优化、多阶段构建、容器网络（bridge/host/none）、容器存储（volume/bind mount） | 能独立编写一个生产级 Dockerfile，理解容器和宿主机的网络关系 |
| 网络基础 | OSI 七层模型、TCP/IP 协议栈、三次握手/四次挥手、DNS 解析流程、NAT/iptables 基础、HTTP 与 HTTPS 基本原理 | 能用 tcpdump 抓包分析 DNS 或 HTTP 请求，理解 iptables 规则的基本作用 |

> **为什么 Docker 比 k8s 先学**：k8s 的 Pod 本质上是一组共享 Namespace 的容器。如果你不懂容器本身，学习 Pod 时就无法理解"为什么需要 Pause 容器"、"Init 容器和 Sidecar 是怎么工作的"等核心概念。  
> **为什么网络比 k8s 先学**：k8s 的 Service 本质上是 iptables 或 ipvs 规则；Ingress 本质上是反向代理；CNI 本质上是虚拟网络。没有网络基础，学习这些概念时只能死记，无法理解原理。

---

### 阶段二：k8s 入门（第 5-8 周）

**目标**：理解 Kubernetes 的核心架构和工作机制，能完成日常运维操作。这是整个学习路线的核心主线。

| 模块 | 核心内容 | 掌握标准 |
|:-----|:---------|:---------|
| 集群架构 | 控制面组件（kube-apiserver、etcd、kube-scheduler、kube-controller-manager、cloud-controller-manager）和节点组件（kubelet、kube-proxy、容器运行时） | 能画出集群架构图，知道每个组件的启动参数和日志位置 |
| 资源对象 | Pod、Deployment、ReplicaSet、StatefulSet、DaemonSet、Job/CronJob、Service、Ingress、ConfigMap、Secret、Namespace | 能独立编写每个资源对象的 YAML，理解它们的作用和关系 |
| 网络 | Service 类型（ClusterIP/NodePort/LoadBalancer/ExternalName）、DNS 机制（CoreDNS）、CNI 原理（Calico/Flannel 基础）、Ingress 工作原理 | 能在集群内通过 Service 名访问服务，理解不同 Service 类型的使用场景 |
| 存储 | PV/PVC、StorageClass、动态供给（Provisioner）、ConfigMap/Secret 作为卷 | 能为工作负载申请持久化存储，理解存储与 Pod 生命周期关系 |
| 基础运维 | kubectl 常用命令、资源描述/编辑、日志查看、进入容器、资源限制/请求 | 日常运维操作熟练，无需依赖文档 |

> **CKA 重点**：阶段二中的大部分内容都是 CKA 核心考点。特别是资源对象、Service、存储、基础运维操作，是考试中最频繁出现的内容。

---

### 阶段三：k8s 进阶 + DevOps 工具链（第 9-14 周）

**目标**：从"会用 k8s" 到"能基于 k8s 设计架构和运维体系"；同时打通 CI/CD 流水线，实现端到端自动化。

| 模块 | 核心内容 | 掌握标准 |
|:-----|:---------|:---------|
| 调度 | 调度器工作原理、预选/优选算法、NodeSelector/亲和性/反亲和性、污点与容忍、Pod 优先级与抢占 | 能根据业务场景设计调度策略 |
| 资源管理 | ResourceQuota/LimitRange、HPA 水平伸缩（基于 CPU/内存/自定义指标）、VPA 垂直伸缩 | 能为不同租户配置资源限制，配置 HPA 自动伸缩 |
| 安全 | RBAC（Role/ClusterRole/RoleBinding/ClusterRoleBinding）、ServiceAccount、NetworkPolicy、PodSecurity/PodSecurityPolicy、Secrets 管理 | 能设计最小权限模型，能用 NetworkPolicy 控制东西向流量 |
| Jenkins CI/CD | Jenkins 基础（Pipeline、Agent）、与 k8s 集成（Jenkins Kubernetes Plugin）、GitLab CI 基础、Helm 包管理、镜像仓库对接 | 能搭建一条从代码提交到 k8s 部署的完整流水线 |
| 中间件部署 | 有状态服务（StatefulSet）部署、ConfigMap/Secret 管理配置、数据持久化、Service 暴露 | 能在 k8s 上部署 Redis/MySQL/Nginx 并保障可用性 |

> **CKA 重点**：调度（污点/容忍/亲和性）、RBAC 权限配置、NetworkPolicy 是 CKA 的高频考点。  
> ** Jenkins 的位置**：之所以放在阶段三，是因为 Jenkins 的 Pipeline 在 k8s 上执行时，需要你对 Pod/Service/权限有实际理解。否则出问题（如 Agent Pod 起不来、镜像拉取失败）你根本无法定位。

---

### 阶段四：可观测性与故障排查（第 15-18 周）

**目标**：构建完整的监控和排障体系，让系统"透明"、让问题"可发现"。

| 模块 | 核心内容 | 掌握标准 |
|:-----|:---------|:---------|
| 监控 | Prometheus 架构（Server/Exporter/Alertmanager）、Metrics 类型（Counter/Gauge/Histogram）、PromQL 基础查询、Grafana 面板配置、告警规则设计 | 能搭建集群监控体系，配置合理的告警规则（避免告警风暴） |
| 日志 | 容器日志采集（DaemonSet/Fluentd/Fluent Bit）、日志聚合查询（Elasticsearch/Loki）、日志链路追踪 | 能搭建日志采集与查询体系 |
| 故障排查 | Pod 状态诊断（Pending/CrashLoopBackOff/ImagePullBackOff）、调度问题、网络问题（DNS/Service/Ingress）、存储问题、资源耗尽、节点故障 | 能形成结构化排查思路，从现象到根因有清晰的推导路径 |

> **CKA 重点**：故障排查本身就是 CKA 的大题（Troubleshooting 部分占 30%）。需要非常熟悉各种 Pod 状态和 kubelet 日志排查。

---

### 阶段五：CKA 认证冲刺（第 19-22 周）

**目标**：通过 CKA 认证，同时验证过去几个月的学习成果。

| 模块 | 核心内容 | 掌握标准 |
|:-----|:---------|:---------|
| 考试范围 | 集群架构（25%）、工作负载与调度（15%）、服务与网络（20%）、存储（10%）、故障排查（30%） | 按考试权重分配复习时间 |
| 高频操作 | kubectl 命令、资源创建/编辑/删除、JSONPath 查询、集群升级、etcd 备份与恢复、节点加入/移除 | 在限定时间内完成操作，熟悉考试环境（Ubuntu 终端） |
| 实战演练 | 模拟考试环境限时刷题、手写 YAML 熟练度、排障场景复盘 | 能在 2 小时内完成模拟考试并取得 80 分以上 |

---

## 四、Kubernetes 从基础到进阶的完整模块拆分

### 4.1 核心概念（必须掌握）

| 概念 | 说明 | 为什么重要 |
|:-----|:-----|:-----------|
| Pod | k8s 的最小调度单元，一个 Pod 包含一个或多个共享 Namespace 的容器 | 所有工作负载的基础，理解 Pod 就理解了 k8s 的调度模型 |
| Controller | 控制循环，通过对比期望状态和实际状态驱动资源收敛 | Deployment/StatefulSet/DaemonSet 等控制器的核心机制 |
| Label | 键值对标签，用于资源筛选和选择 | Service 选择 Pod、调度亲和性、资源分组都依赖 Label |
| Selector | 通过 Label 选择目标资源的匹配规则 | 理解 Service 如何找到 Pod、Deployment 如何管理 ReplicaSet 的关键 |
| Namespace | 资源隔离的虚拟集群，用于多租户或环境隔离 | 生产环境必须基于 Namespace 做隔离和权限管理 |
| ResourceQuota | 限制一个 Namespace 内资源总量 | 防止单个租户耗尽集群资源 |

### 4.2 控制面组件（必须理解）

| 组件 | 职责 | 常见问题 |
|:-----|:-----|:---------|
| **kube-apiserver** | 暴露 k8s API，所有操作（kubectl、控制器、节点）都通过它 | 高可用通常部署多个实例，通过负载均衡对外服务 |
| **etcd** | 分布式键值存储，保存所有集群状态和配置数据 | 数据备份和恢复是 CKA 重点，etcd 故障会导致集群不可用 |
| **kube-scheduler** | 负责 Pod 调度到合适的节点 | 调度失败通常与资源不足、污点/亲和性不匹配有关 |
| **kube-controller-manager** | 运行各种控制器（Deployment、ReplicaSet、Node 等），维护集群状态 | 控制器停止后，资源将无法正常创建/更新/删除 |
| **cloud-controller-manager** | 与云厂商 API 对接，管理云资源（如负载均衡、存储卷） | 自建集群时可能不需要 |

### 4.3 节点组件（必须理解）

| 组件 | 职责 | 常见问题 |
|:-----|:-----|:---------|
| **kubelet** | 节点上的代理，接收 API 指令并管理 Pod 生命周期（启动/停止/监控容器） | kubelet 故障导致节点变 NotReady，Pod 被驱逐 |
| **kube-proxy** | 实现 Service 网络规则（iptables 或 ipvs），负责流量转发 | 服务无法访问时，检查 kube-proxy 日志和 iptables 规则 |
| **容器运行时** | 运行容器（containerd/CRI-O），接收 kubelet 的 CRI 指令 | 容器无法启动时，检查 runtime 状态和镜像可用性 |

### 4.4 常见资源对象（必须逐项掌握）

#### 工作负载（Workload）

| 资源 | 用途 | 适用场景 |
|:-----|:-----|:---------|
| **Pod** | 最小调度单元，直接运行容器 | 极少直接创建，通常由控制器管理 |
| **Deployment** | 管理无状态应用，支持滚动更新和回滚 | 绝大多数 Web 服务、API 服务 |
| **StatefulSet** | 管理有状态应用，提供稳定的网络标识和存储 | 数据库、消息队列等需要持久化的服务 |
| **DaemonSet** | 确保每个节点（或部分节点）运行一个 Pod 副本 | 日志采集、监控 Agent、网络插件 |
| **Job** | 运行一次性任务，完成后退出 | 数据迁移、批处理 |
| **CronJob** | 定时触发 Job | 定时备份、定时清理 |
| **ReplicaSet** | 确保指定数量的 Pod 副本运行 | 通常由 Deployment 管理，不直接使用 |

#### 服务与网络（Service & Ingress）

| 资源 | 用途 | 关键理解 |
|:-----|:-----|:---------|
| **Service** | 为一组 Pod 提供稳定的访问入口和负载均衡 | 本质是 kube-proxy 维护的 iptables/ipvs 规则，不是实际进程 |
| **Ingress** | 提供 HTTP/HTTPS 七层路由，将外部流量按域名/路径分发到不同 Service | 需要 Ingress Controller（如 Nginx Ingress）才能工作 |
| **NetworkPolicy** | 定义 Pod 之间的网络访问策略（东西向流量控制） | 需要 CNI 插件支持（Calico 支持，Flannel 不支持） |
| **Endpoints** | 记录 Service 后端 Pod 的 IP 和端口列表 | 自动由 Service 和 Label Selector 维护 |
| **EndpointSlice** | 替代 Endpoints，用于大规模集群下的服务发现 | 1.21+ 默认使用 |

#### 配置与存储（Config & Storage）

| 资源 | 用途 | 关键理解 |
|:-----|:-----|:---------|
| **ConfigMap** | 存储非敏感配置，以环境变量或卷的形式注入容器 | 适合存储配置参数，但不适合存储大量数据 |
| **Secret** | 存储敏感数据（密码、Token、密钥） | 默认以 Base64 编码（并非加密），需配合 RBAC 限制访问 |
| **PV** | 持久卷（PersistentVolume），集群级别的存储资源 | 由管理员预先创建，或通过动态供给自动创建 |
| **PVC** | 持久卷申领（PersistentVolumeClaim），Pod 对存储的申请 | 绑定后 Pod 可以挂载使用，生命周期与 Pod 独立 |
| **StorageClass** | 定义存储的"模板"和动态供给参数 | 不同 StorageClass 对应不同存储后端（如 NFS、Ceph、云盘） |
| **Volume** | Pod 级别的存储挂载机制 | 支持多种类型：emptyDir、hostPath、configMap、secret、pvc 等 |

#### 权限与安全（RBAC & Security）

| 资源 | 用途 | 关键理解 |
|:-----|:-----|:---------|
| **ServiceAccount** | 为 Pod 提供身份认证，Pod 默认使用 default SA | 不同应用应使用不同 SA，并绑定不同权限 |
| **Role** | 定义在某一 Namespace 内的权限规则 | 权限最小化原则，只授予必要的 API 操作权限 |
| **ClusterRole** | 定义集群级别的权限规则 | 用于需要跨 Namespace 或访问集群级资源的场景 |
| **RoleBinding** | 将 Role 绑定到用户/组/ServiceAccount（限定在 Namespace） | 实现 Namespace 内的权限分配 |
| **ClusterRoleBinding** | 将 ClusterRole 绑定到用户/组/ServiceAccount（集群级别） | 谨慎使用，防止权限过大 |
| **PodSecurityPolicy** (废弃) | 定义 Pod 安全策略（如禁止特权容器） | 1.25+ 废弃，被 Pod Security Admission 替代 |
| **PodSecurityContext** | 定义 Pod 级别的安全上下文（如运行用户、只读文件系统） | 安全加固的重要手段 |

### 4.5 调度（Scheduling）

| 机制 | 说明 | 使用场景 |
|:-----|:-----|:---------|
| **NodeSelector** | 最简单的节点选择，通过 Label 匹配节点 | 简单场景，指定 Pod 运行在特定节点上 |
| **节点亲和性** | requiredDuringSchedulingIgnoredDuringExecution（硬约束）和 preferredDuringSchedulingIgnoredDuringExecution（软约束） | 需要 Pod 尽量或必须运行在特定节点上 |
| **Pod 亲和性/反亲和性** | 控制 Pod 之间的位置关系（同节点/不同节点） | 高可用部署（反亲和性）或共享缓存（亲和性） |
| **污点（Taint）** | 节点对 Pod 的"排斥"标记 | 特殊节点标记（如 GPU 节点、master 节点） |
| **容忍（Toleration）** | Pod 对污点的"容忍"标记，允许被调度到带污点的节点 | 只有带有容忍的 Pod 才能调度到带污点节点 |
| **Pod 优先级** | 定义 Pod 的调度优先级，高优先级可以抢占低优先级的 Pod | 资源紧张时保障核心服务优先运行 |
| **Pod 抢占** | 高优先级 Pod 驱逐低优先级 Pod 以获取资源 | 配合优先级使用，保障关键业务 |

### 4.6 网络（Network）

| 主题 | 关键内容 | 理解要点 |
|:-----|:---------|:---------|
| **Service 网络** | ClusterIP（集群内虚拟 IP）、NodePort（暴露节点端口）、LoadBalancer（云厂商负载均衡）、ExternalName（DNS 别名） | 四种类型分别对应不同暴露场景，核心是 ClusterIP 的虚拟 IP 机制 |
| **DNS 机制** | CoreDNS 作为集群 DNS 服务器，为 Service 提供 A 记录和 SRV 记录 | 跨 Namespace 访问格式：`svc-name.namespace.svc.cluster.local` |
| **CNI 插件** | Calico（BGP/Overlay 网络，支持 NetworkPolicy）、Flannel（简单 Overlay，不支持 NetworkPolicy）、Cilium（eBPF 高性能） | 选择 CNI 时考虑功能需求（是否需要策略）和性能要求 |
| **Ingress 工作流程** | 外部请求 → Ingress Controller（Nginx/Traefik）→ 根据 Ingress 规则路由到 Service → 转发到后端 Pod | Ingress 本身不处理流量，需要 Controller 配合 |
| **集群 DNS** | CoreDNS 配置、Pod 的 DNS 配置（dnsPolicy、dnsConfig） | 自定义 DNS 解析或外网解析问题排查的关键 |

### 4.7 存储（Storage）

| 主题 | 关键内容 | 理解要点 |
|:-----|:---------|:---------|
| **存储生命周期** | 静态供给（手动创建 PV）和动态供给（StorageClass + Provisioner 自动创建 PV） | 生产环境通常使用动态供给 |
| **访问模式** | ReadWriteOnce（单节点读写）、ReadOnlyMany（多节点只读）、ReadWriteMany（多节点读写） | 不同存储后端支持不同模式，云盘通常只支持 RWO |
| **状态性 vs 无状态** | Deployment 配合 PVC 为有状态；StatefulSet 为强有状态（每 Pod 独立存储） | 数据库等需要稳定存储标识的用 StatefulSet |
| **存储类（StorageClass）** | 定义存储后端的参数，如 NFS、Ceph RBD、云盘 EBS/SSD | 不同业务选择不同性能和价格的存储类 |

### 4.8 安全（Security）

| 主题 | 关键内容 | 理解要点 |
|:-----|:---------|:---------|
| **RBAC 权限模型** | 基于角色的最小权限访问控制，主体（User/SA/Group）→ 绑定 → 角色（Role/ClusterRole）→ 规则（Verbs + Resources） | 权限设计原则：只给必要权限，ServiceAccount 粒度化 |
| **NetworkPolicy** | 定义 Pod 级别的网络隔离策略（入站和出站规则） | 默认放行所有流量，需要显式定义策略来收紧 |
| **Secret 安全** | Secret 存储（Base64 编码，非加密）、外部 Secret 管理（如 HashiCorp Vault）、RBAC 限制 Secret 读取 | 不要直接在 YAML 中硬编码敏感信息 |
| **Pod 安全上下文** | SecurityContext（runAsNonRoot、readOnlyRootFilesystem、capabilities、seccompProfile） | 加固容器的安全基线 |

### 4.9 监控与日志（Observability）

| 主题 | 关键内容 | 理解要点 |
|:-----|:---------|:---------|
| **Prometheus 架构** | Server（拉取和存储指标）、Exporter（暴露指标）、Alertmanager（告警管理）、Pushgateway（短生命周期任务） | 拉取模型（Pull）需要 Pod 暴露 `/metrics` 端点 |
| **Metrics 类型** | Counter（累加计数器）、Gauge（瞬时值）、Histogram（直方图，含分位数）、Summary（摘要） | 选择合适的类型才能正确查询和告警 |
| **Grafana** | 可视化面板，数据源对接 Prometheus，支持 PromQL 查询 | 告警用 Alertmanager，可视化用 Grafana |
| **日志采集** | DaemonSet 部署的 Fluent Bit/Fluentd 采集节点日志，推送到 Elasticsearch 或 Loki | 容器日志是标准输出，采集器需要读取节点上的日志文件 |
| **资源指标** | Metrics Server（kubectl top 数据源）、HPA 依赖的资源指标 | Metrics Server 只提供资源使用快照，不保存历史数据 |

### 4.10 故障排查（Troubleshooting）

| 场景 | 排查思路 | 常用命令 |
|:-----|:---------|:---------|
| **Pod 一直 Pending** | 资源不足（CPU/内存/磁盘）→ 节点污点/亲和性不匹配 → PVC 未绑定 → 镜像拉取失败 | `kubectl describe pod`、`kubectl describe node`、`kubectl get events` |
| **Pod 反复 CrashLoopBackOff** | 容器启动失败（入口命令错误、依赖缺失）→ 健康检查失败（探针配置错误）→ 资源限制（OOMKilled） | `kubectl logs --previous`、`kubectl describe pod` |
| **Pod 状态 ImagePullBackOff** | 镜像名拼写错误、镜像不存在、镜像仓库无权限、网络不通 | `kubectl describe pod` 查看事件详情 |
| **服务无法访问** | Pod 是否正常 → Service 是否正确选择 Pod（Label/Selector）→ kube-proxy 规则是否正常 → Ingress 规则是否正确 → DNS 解析是否成功 | `kubectl get endpoints`、`kubectl describe svc`、`kubectl describe ingress` |
| **节点 NotReady** | kubelet 状态 → 网络连接（CNI 插件）→ 资源耗尽（磁盘/内存）→ 节点时钟不同步 | `kubectl describe node`、`systemctl status kubelet`、`journalctl -u kubelet` |
| **权限不足** | ServiceAccount 权限 → RBAC 绑定 → Role 规则是否包含所需资源 | `kubectl auth can-i` 验证权限 |

### 4.11 集群运维（Cluster Ops）

| 主题 | 关键内容 | 理解要点 |
|:-----|:---------|:---------|
| **集群升级** | 控制面升级（逐个组件升级）和节点升级（kubelet/kube-proxy） | 遵循官方版本兼容矩阵，通常先升级 master 再升级 node |
| **etcd 备份与恢复** | etcd 快照（`etcdctl snapshot save`）和恢复 | CKA 必考，etcd 是集群的数据底座 |
| **节点加入/移除** | kubeadm join 加入节点、kubectl drain 优雅驱逐 + cordon 标记 | drain 操作会驱逐 Pod 并标记节点不可调度 |
| **证书管理** | kubeadm 生成的证书有效期（默认 1 年），证书续期方法 | 证书过期会导致组件间通信失败，集群不可用 |
| **资源配额管理** | ResourceQuota（限制 Namespace 资源总量）、LimitRange（限制 Pod/容器的默认资源） | 多租户集群必须配置，防止资源抢占 |
| **HPA/VPA** | HPA 水平伸缩（根据指标增加/减少 Pod 数量）、VPA 垂直伸缩（调整 Pod 资源请求） | HPA 使用广泛，VPA 需要谨慎使用（会导致 Pod 重启） |

---

## 五、服务类型、常见组件及它们之间的关系

### 5.1 Kubernetes 服务类型

| 服务类型 | 访问方式 | 内部实现 | 典型场景 |
|:---------|:---------|:---------|:---------|
| **ClusterIP** | 集群内访问：`svc-name.namespace` | kube-proxy 创建虚拟 IP，通过 iptables/ipvs 转发到后端 Pod | 集群内部服务间通信 |
| **NodePort** | 节点 IP + 端口：`NodeIP:NodePort` | 在每个节点上开放相同端口，映射到 ClusterIP | 开发测试、临时暴露 |
| **LoadBalancer** | 云厂商提供的外部负载均衡 IP | 由云厂商的控制器自动创建外部 LB 并指向 NodePort/ClusterIP | 生产环境直接暴露服务 |
| **ExternalName** | 返回外部服务的 CNAME 记录 | DNS 别名，不创建代理 | 将集群内请求重定向到外部服务 |
| **Headless Service** | 直接返回后端 Pod IP 列表 | 无 ClusterIP，DNS 直接解析到 Pod IP | 有状态服务（如 StatefulSet）需要直接访问每个 Pod |

### 5.2 常见组件与工具链

#### 基础设施与容器

| 组件 | 角色 | 与 k8s 的关系 |
|:-----|:-----|:--------------|
| **containerd** | 容器运行时 | k8s 通过 CRI 接口调用 containerd 运行容器 |
| **Docker** | 容器引擎/镜像构建工具 | k8s 1.24+ 移除了内置 Docker 支持，使用 containerd 作为默认运行时；但 Docker 仍是最常用的镜像构建工具 |
| **etcd** | 分布式键值存储 | k8s 控制面的数据存储，所有集群状态保存在 etcd |
| **CNI 插件** | 容器网络接口实现 | 为 Pod 分配 IP、建立网络连接（Calico/Flannel/Cilium） |

#### 服务入口与网关

| 组件 | 角色 | 与 k8s 的关系 |
|:-----|:-----|:--------------|
| **Nginx Ingress Controller** | 实现 Ingress 规则的七层代理 | 监听 Ingress 资源变化，动态生成 Nginx 配置，实现 HTTP 路由 |
| **Traefik** | 轻量级 Ingress Controller | 自动服务发现，配置更新无需重启 |
| **MetalLB** | 裸机集群的 LoadBalancer 实现 | 为自建集群提供 LoadBalancer 类型的 Service 能力 |

#### 存储

| 组件 | 角色 | 与 k8s 的关系 |
|:-----|:-----|:--------------|
| **NFS Provisioner** | NFS 动态供给插件 | 为 PVC 自动创建基于 NFS 的 PV |
| **Ceph RBD / CephFS** | 分布式存储后端 | 通过 CSI 插件为 k8s 提供块存储和文件存储 |
| **Rook** | Ceph 的 k8s 编排器 | 用 k8s 的方式部署和管理 Ceph 集群 |

#### 监控与可观测性

| 组件 | 角色 | 与 k8s 的关系 |
|:-----|:-----|:--------------|
| **Prometheus** | 时序数据库，收集和存储指标 | 通过 ServiceMonitor 自动发现 k8s 服务，拉取指标 |
| **Grafana** | 可视化仪表盘 | 对接 Prometheus 数据源，展示 k8s 集群和应用监控 |
| **Alertmanager** | 告警管理与通知 | 接收 Prometheus 告警，进行分组、抑制、路由和通知 |
| **Fluent Bit / Fluentd** | 日志采集器 | 以 DaemonSet 运行在每个节点上，采集容器日志 |
| **Loki** | 轻量级日志聚合系统 | 专门为 k8s 日志设计，与 Grafana 深度集成 |
| **Jaeger** | 分布式链路追踪系统 | 追踪跨服务的请求链路，定位性能瓶颈 |
| **Metrics Server** | 资源指标采集器 | 为 kubectl top 和 HPA 提供资源使用数据（非历史数据） |

#### 包管理与部署

| 组件 | 角色 | 与 k8s 的关系 |
|:-----|:-----|:--------------|
| **Helm** | k8s 包管理工具（类似 apt/yum） | 将一组 k8s 资源打包为 Chart，支持版本管理和参数化配置 |
| **Kustomize** | 原生配置管理工具 | 无需模板语言，通过 overlay 机制管理不同环境的配置差异 |
| **ArgoCD** | GitOps 持续交付工具 | 将 Git 仓库作为"唯一事实来源"，自动同步 k8s 集群状态 |

#### 安全与镜像

| 组件 | 角色 | 与 k8s 的关系 |
|:-----|:-----|:--------------|
| **Harbor** | 企业级镜像仓库 | 存储镜像、镜像签名、安全扫描、权限管理 |
| **Cert-Manager** | 证书自动管理 | 自动为 Ingress 申请和续期 TLS 证书（Let's Encrypt 或自签 CA） |
| **Falco** | 运行时安全监控 | 检测容器中的异常行为（如 shell 执行、敏感文件读取） |
| **OPA (Open Policy Agent)** | 策略即代码引擎 | 为 k8s 提供准入控制（Admission Control），强制执行自定义策略 |

#### 组件关系总览

```
                        ┌─────────────────┐
                        │    用户请求      │
                        └────────┬────────┘
                                 │
              ┌──────────────────┼──────────────────┐
              │                  │                  │
              ▼                  ▼                  ▼
    ┌──────────────┐    ┌──────────────┐    ┌──────────────┐
    │  Nginx Ingress │    │ LoadBalancer │    │   NodePort   │
    │  Controller    │    │ (云厂商 LB)  │    │              │
    └──────┬───────┘    └──────┬───────┘    └──────┬───────┘
           │                   │                   │
           └───────────────────┼───────────────────┘
                               │
                               ▼
                    ┌──────────────────┐
                    │ Service (ClusterIP) │  ← kube-proxy 维护 iptables/ipvs 规则
                    └────────┬─────────┘
                             │
               ┌─────────────┼─────────────┐
               ▼             ▼             ▼
         ┌────────┐   ┌────────┐   ┌────────┐
         │ Pod 1  │   │ Pod 2  │   │ Pod 3  │  ← containerd 运行容器
         │(app)   │   │(app)   │   │(app)   │
         └────┬───┘   └────┬───┘   └────┬───┘
              │            │            │
              ▼            ▼            ▼
         ┌────────┐   ┌────────┐   ┌────────┐
         │  PVC   │   │  PVC   │   │  PVC   │  ← 通过 StorageClass 动态供给 PV
         └────────┘   └────────┘   └────────┘

 控制面：kube-apiserver ←→ etcd (存储所有状态)
         kube-scheduler (调度 Pod 到节点)
         kube-controller-manager (维护资源状态)
         cloud-controller-manager (对接云厂商)

 节点：  kubelet (管理节点上的 Pod)
         kube-proxy (维护 Service 网络规则)
         CNI 插件 (Calico/Flannel) (为 Pod 分配网络)
         容器运行时 (containerd) (运行容器)
```

---

## 六、学习先后关系：Jenkins、网络、容器与 k8s 的先后顺序

### 6.1 依赖关系图

```
Linux 基础
    │
    ├─→ Docker 容器基础
    │       │
    │       ├─→ Kubernetes 核心（必须依赖容器理解）
    │       │       │
    │       │       ├─→ k8s 进阶（调度、网络、存储、安全）
    │       │       │       │
    │       │       │       ├─→ 监控与可观测性（需要集群环境）
    │       │       │       │       │
    │       │       │       │       └─→ 故障排查（必须有真实问题练手）
    │       │       │       │
    │       │       │       └─→ Jenkins CI/CD（在理解容器和 k8s 后更顺畅）
    │       │       │
    │       │       └─→ Helm / Kustomize（在理解资源对象后学习）
    │       │
    │       └─→ 容器镜像仓库（Harbor）
    │
    └─→ 网络基础
            │
            ├─→ k8s 网络（Service/CNI/Ingress，必须依赖网络基础）
            │
            └─→ 网络安全（NetworkPolicy，iptables）
```

### 6.2 为什么这样排序

| 顺序 | 内容 | 原因 |
|:-----|:-----|:-----|
| 先学 **Docker** 再学 **k8s** | k8s 的 Pod 是容器的编排层，Sidecar/Init 容器、共享 Namespace、Pause 容器的概念都建立在容器理解之上。 | 如果没有容器基础，学习 Pod 时只能死记，无法理解"为什么 Pod 要这样设计"。 |
| 先学 **网络** 再学 **k8s Service** | Service 的虚拟 IP 是 kube-proxy 通过 iptables/ipvs 实现的；Ingress 本质是反向代理；CNI 是虚拟网络。 | 没有网络基础，学习 Service 和 Ingress 时无法真正理解流量是怎么转发的。 |
| 先学 **k8s 基础** 再学 **Jenkins** | Jenkins on k8s 的 Pipeline 中，Agent 是以 Pod 形式运行的。如果不懂 Pod 生命周期、资源限制、权限，出了问题完全无法排查。 | Jenkins 在 k8s 上出问题的根因，90% 是 k8s 本身的问题（镜像拉取失败、资源不足、权限不足）。 |
| 先学 **k8s 资源对象** 再学 **Helm** | Helm 是资源对象的模板化和打包工具。如果不理解 Pod/Deployment/Service，Helm 只是"又一层抽象"。 | 先理解"裸资源"怎么写，再用 Helm 提高效率，才能知道 Helm 帮你做了什么。 |
| 先学 **k8s 基础** 再学 **监控** | Prometheus 监控的指标需要理解 k8s 的资源模型（Pod/Container/Node），告警规则需要理解 k8s 的异常状态。 | 监控体系是"应用层"，k8s 是"平台层"，没有平台理解，监控配置就是盲目照抄。 |

> **一句话总结**：`Linux 和 Docker 是地基，网络是骨架，k8s 是主体，Jenkins 和监控是装修，安全和优化是软装。地基和骨架不稳，后面的东西都是虚的。`

---

## 七、现实可行的学习节奏（按周/按月安排）

> 假设每周投入 **10-12 小时**（工作日每天 1.5-2 小时，周末各 2-3 小时）。如果时间不足，每个阶段可按比例延长。

### 阶段一：筑基期（第 1-4 周）

| 周次 | 学习内容 | 产出物 |
|:-----|:---------|:-------|
| 第 1 周 | Linux 核心：进程管理、文件系统、cgroups/namespace | 能手动用 cgroups 限制一个进程的 CPU/内存；理解 namespace 隔离效果 |
| 第 2 周 | Docker 基础：镜像、容器、生命周期、Dockerfile | 独立编写一个多阶段构建的 Dockerfile，构建并运行一个 Web 应用容器 |
| 第 3 周 | Docker 进阶：容器网络（bridge/host）、存储（volume/bind mount）、docker-compose | 用 docker-compose 部署一个 Web + DB 的多容器应用，验证网络连通性 |
| 第 4 周 | 网络基础：TCP/IP、DNS、NAT、iptables、四层/七层 | 能用 tcpdump 抓包分析 HTTP 请求；理解 iptables 规则如何影响容器网络 |

### 阶段二：k8s 入门（第 5-8 周）

| 周次 | 学习内容 | 产出物 |
|:-----|:---------|:-------|
| 第 5 周 | 用 kubeadm 手动搭建一个集群（1 master + 2 worker）；理解每个组件的启动参数和日志 | 一个可用的测试集群，能通过 kubectl 操作 |
| 第 6 周 | 核心资源对象：Pod、Deployment、Service、ConfigMap、Secret；kubectl 日常操作 | 能在集群上部署一个无状态的 Web 应用（如 Nginx），通过 Service 访问 |
| 第 7 周 | 存储（PV/PVC/StorageClass）+ StatefulSet（有状态应用）+ DaemonSet | 为有状态应用（如 Redis）配置持久化存储，部署 DaemonSet 日志采集器 |
| 第 8 周 | 网络深入：CNI 插件（Calico/Flannel）、Ingress（Nginx Ingress Controller）、DNS | 配置 Ingress 实现域名路由，验证跨 Namespace DNS 访问 |

### 阶段三：k8s 进阶 + DevOps（第 9-14 周）

| 周次 | 学习内容 | 产出物 |
|:-----|:---------|:-------|
| 第 9 周 | 调度：NodeSelector、亲和性/反亲和性、污点/容忍 | 设计一个调度方案，让应用 Pod 避开某些节点、尽量分散在不同节点 |
| 第 10 周 | 安全：RBAC（Role/ClusterRole/Binding）、ServiceAccount、NetworkPolicy | 为不同 Namespace 配置独立的权限体系，配置 NetworkPolicy 隔离流量 |
| 第 11 周 | 资源管理：ResourceQuota/LimitRange、HPA（水平伸缩） | 配置 HPA 让应用根据 CPU 自动伸缩，配置资源配额防止资源抢占 |
| 第 12 周 | Jenkins 基础 + Pipeline：Jenkins 安装、Pipeline 语法、Agent 配置 | 搭建一条从 Git 拉取到镜像构建的 Jenkins 流水线 |
| 第 13 周 | Jenkins + k8s 集成：Kubernetes Plugin、Helm 部署、滚动更新 | 流水线从镜像构建到 k8s 自动部署，实现 Git 推送即部署 |
| 第 14 周 | Helm + Kustomize：包管理、参数化配置、多环境部署 | 将一个应用打包为 Helm Chart，支持 dev/test/prod 多环境配置 |

### 阶段四：可观测性（第 15-18 周）

| 周次 | 学习内容 | 产出物 |
|:-----|:---------|:-------|
| 第 15 周 | Prometheus 监控体系：部署、ServiceMonitor、基础告警规则 | 集群部署 Prometheus，采集 k8s 节点和容器指标 |
| 第 16 周 | Grafana 面板 + Alertmanager 告警：可视化、告警分组/路由/通知 | 配置 Grafana 面板展示集群状态，配置告警通知到邮件/钉钉 |
| 第 17 周 | 日志采集：Fluent Bit + Loki / ELK，日志查询和告警 | 集群日志统一采集，能通过关键词检索应用日志 |
| 第 18 周 | 故障排查：模拟常见故障（网络不通、Pod 调度失败、资源不足），实战排查 | 能独立处理 5-8 种常见故障场景，形成排查 SOP |

### 阶段五：CKA 认证冲刺（第 19-22 周）

| 周次 | 学习内容 | 产出物 |
|:-----|:---------|:-------|
| 第 19 周 | 按考试权重逐一过：集群架构（25%）、工作负载与调度（15%） | 完成所有相关练习，确保每道题 5 分钟内完成 |
| 第 20 周 | 服务与网络（20%）、存储（10%） | 熟练配置 Service/Ingress/NetworkPolicy/PVC，限时完成 |
| 第 21 周 | 故障排查（30%）+ 高频操作强化 | 能在限定时间内快速定位并修复集群故障 |
| 第 22 周 | 模拟考试（2 小时限时）+ 查漏补缺 | 连续 2 次模拟考试达到 80 分以上，预约正式考试 |

> **总共约 5-6 个月**，从筑基到拿证。如果每周时间不足，可延长至 8-10 个月。重点是：每个阶段学完必须有"产出物"（部署的东西、写的 YAML、搭的环境），不要只看书不动手。

---

## 八、内容分类：先学 / 后补 / CKA 重点

### 必须先学（前置知识，没有商量余地）

| 内容 | 原因 | 不学的后果 |
|:-----|:-----|:-----------|
| Linux 进程、文件系统、网络 | k8s 运行在 Linux 上，容器依赖 Linux 内核特性 | 看不懂容器隔离机制，无法理解 Pod 行为 |
| Docker 容器基础 | k8s 是容器的编排平台，Pod 由容器组成 | 只能死记 k8s 命令，无法理解资源状态 |
| 网络基础（TCP/IP、DNS、NAT） | k8s 的网络模型完全依赖这些概念 | Service/Ingress/CNI 的学习完全卡住 |
| k8s 控制面和节点组件 | 理解集群如何工作 | 集群出问题完全无法排查 |
| Pod 和核心控制器 | 所有工作负载的基础 | 无法部署和管理应用 |
| Service 和 DNS | 服务发现和访问入口 | 应用无法通信，服务无法暴露 |

### 可以后补（前置知识不足时，可先跳过）

| 内容 | 跳过条件 | 建议补学时机 |
|:-----|:---------|:-------------|
| 高级调度（Pod 优先级/抢占） | 单集群、单租户场景 | 多租户或资源紧张时 |
| VPA 垂直伸缩 | 有 HPA 满足大部分场景 | 精细化资源治理时 |
| 链路追踪（Jaeger） | 单体或简单微服务架构 | 大规模微服务架构时 |
| Falco 运行时安全 | 安全基线已通过 RBAC/NetworkPolicy 保障 | 高安全要求场景（金融、政务） |
| OPA 策略引擎 | 准入控制需求不复杂 | 需要自定义准入策略时 |
| ArgoCD GitOps | 有 Jenkins 流水线已满足需求 | 需要 Git 作为唯一事实来源时 |
| 多集群管理 | 单集群即可满足需求 | 业务扩展到多地域/多可用区时 |
| 服务网格（Istio） | 服务间通信简单，无复杂治理需求 | 需要流量治理、安全通信、可观测性时 |

### CKA 考试重点（按权重排序）

| 考试范围 | 权重 | 核心内容 | 建议投入时间 |
|:---------|:----:|:---------|:-------------|
| **集群架构、安装和配置** | 25% | 集群搭建（kubeadm）、etcd 备份/恢复、证书升级、节点加入 | **高**（3-4 周） |
| **故障排查** | 30% | Pod 状态诊断、节点问题排查、日志分析、kubelet 故障 | **最高**（4-5 周） |
| **服务与网络** | 20% | Service 配置、Ingress、NetworkPolicy、DNS、CNI 基础 | **高**（3 周） |
| **工作负载与调度** | 15% | Deployment/StatefulSet/DaemonSet、调度策略（污点/亲和性）、HPA | **中**（2 周） |
| **存储** | 10% | PV/PVC、StorageClass、动态供给、访问模式 | **中**（1-2 周） |

> **CKA 考试特点**：实操考试，没有选择题。所有操作在命令行完成，需要熟练手写 YAML、熟练 kubectl 命令。考试时允许访问官方文档，但时间有限（2 小时），必须对操作极其熟练，不能依赖查文档。  
> **建议**：在考试前 2-3 周，每天进行 2 小时限时模拟，强化手写速度和操作熟练度。

---

## 九、总结：执行路径

### 第一步：选定当前阶段（自查）

请对照下表，诚实评估自己当前处于哪个阶段：

| 检查项 | 能独立完成 | 需要看文档 | 完全不会 |
|-------|:----------:|:--------:|:------:|
| 用 Docker 构建一个多阶段镜像并运行 | ☐ | ☐ | ☐ |
| 用 docker-compose 部署多容器应用 | ☐ | ☐ | ☐ |
| 理解 TCP/IP 三次握手和 DNS 解析流程 | ☐ | ☐ | ☐ |
| 手动搭建一个 kubeadm 集群 | ☐ | ☐ | ☐ |
| 独立编写 Deployment + Service + PVC 的 YAML | ☐ | ☐ | ☐ |
| 配置 Ingress 实现域名路由 | ☐ | ☐ | ☐ |
| 用 RBAC 配置最小权限模型 | ☐ | ☐ | ☐ |
| 配置 HPA 自动伸缩 | ☐ | ☐ | ☐ |
| 搭建 Prometheus + Grafana 监控体系 | ☐ | ☐ | ☐ |
| 能排查 Pod 调度失败、网络不通、镜像拉取失败 | ☐ | ☐ | ☐ |

> **如果前 3 项有"完全不会"**：请从阶段一开始。  
> **如果第 4-6 项有"完全不会"**：请从阶段二开始。  
> **如果第 7-10 项有"完全不会"**：请从阶段三或四开始。  
> **如果全部能独立完成**：直接进入阶段五，准备 CKA 考试。

### 第二步：按阶段推进，拒绝并行

```
当前阶段（专注） → 产出物验证（确认掌握） → 进入下一阶段 → 循环
```

> 不要同时推进多个阶段。你当前精力分散的根源就是试图同时学习 Linux、Docker、k8s、Jenkins，导致每个方向都浅尝辄止。选一个阶段，学透，再推进。

### 第三步：每个知识点必须产出验证

学习不是看完文档就结束，必须有产出：

| 学习内容 | 产出验证方式 |
|:---------|:------------|
| Docker 容器 | 构建一个镜像，运行容器，验证网络/存储功能 |
| k8s 资源对象 | 手写 YAML 部署，验证实际运行状态 |
| k8s 网络 | 在 Pod 内通过 Service 名访问另一个服务，抓包验证 |
| 故障排查 | 故意制造故障，按照排查流程定位并修复 |
| 监控体系 | 部署后，观察真实指标，配置一个有效告警 |
| Jenkins 流水线 | 从代码提交到 k8s 部署，全流程走通一次 |

### 第四步：以 CKA 为里程碑，但不以考证为终点

CKA 是一个很好的里程碑，但真正的目标是能力。通过 CKA 考试后，建议继续学习：

- 安全加固（Falco、OPA、安全上下文）
- 成本治理（资源配额、闲置清理、FinOps）
- 多集群管理（Karmada、Rancher）
- 服务网格（Istio）
- 自动化运维（Operator、CRD）

---

> **最后提醒**：这份路线规划的前提是"每周投入 10-12 小时"。如果时间不足，就按比例延长周期，但千万不要压缩每个阶段的最低投入——每个阶段少了，产出验证就不充分，下一阶段会加倍卡壳。不要急，扎实比快重要。
